package domain

const (
	StoreMemory string = "memory" // Дефолтный адрес запуска HTTP-сервера
	StoreDB     string = "DB"     // Дефолтный базовый адрес результирующего сокращённого URL
	StoreFile   string = "file"   // Дефолтный адрес файла с ссылками
)
